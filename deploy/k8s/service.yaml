# HTTP + WebSocket only. The UDP telemetry ingress does NOT go through a Service — the console
# sends to the node IP (<node-ip>:20777) and reaches the pod via hostPort on the container.
apiVersion: v1
kind: Service
metadata:
  name: f126
  namespace: f126
  labels:
    app.kubernetes.io/name: f126
    app.kubernetes.io/part-of: f126-race-engineer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: f126
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
